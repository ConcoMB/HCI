function TranslatorButton(element, languageCode, dictionary) {
	$(element).click(function() {
		if(subscriber.on) {
			view.translateSubsite(languageCode, dictionary, "", true);
			subscriber.on = false;
		}
	})
}