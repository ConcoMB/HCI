function orderItem(product_id, count) {
	this.product_id = product_id;
	this.count = count;
}

function toOrderItemXML() {
	return getDOM("<order_item><product id='" + product_id + "'></product>" + "<count>" + count + "</count></order_item>")
}